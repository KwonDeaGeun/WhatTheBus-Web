import { Camera } from "lucide-react";
import { Button } from "./components/ui/button";

function App() {
    return (
        <div>
            TailwindCSS 적용
            <Button type="button">버튼</Button>
            <Camera />
        </div>
    );
}

export default App;
